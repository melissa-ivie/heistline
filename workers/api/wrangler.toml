name = "heistline-api"
main = "src/index.ts"
compatibility_date = "2026-01-08"
account_id = "8093b7c1c16a0842e3397a717a55f96d"

# Custom domain — serves the API at https://api.heistline.org
routes = [
  { pattern = "api.heistline.org", custom_domain = true }
]

# D1 Database binding
[[d1_databases]]
binding = "DB"
database_name = "heistline-db"
database_id = "b20c2562-1bd3-42d1-8a5d-12097537299d"

# Secrets (set via `wrangler secret put <NAME>`, not committed):
# STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, HEISTLINE_ACCESS_TOKEN
